struct VertexOutput
{
    float4 position : SV_Position;
    float3 color : COLOR0;
};

struct DrawIndexedIndirectCommand {
    uint indexCount;
    uint instanceCount;
    uint firstIndex;
    int vertexOffset;
    uint firstInstance;
};

struct MeshInfo {
    uint indexCount;
    uint firstIndex;
    int vertexOffset;
};

struct RenderObject {
    float4x4 model;
    uint meshID;
};